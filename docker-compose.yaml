version: '3.8'

services:
  crm:
    build: .
    ports:
      - "8010:8010"
    volumes:
      - ./app:/app/app
      - ./crm.db:/app/crm.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
