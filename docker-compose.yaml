version: '3.8'

services:
  crm:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./crm.db:/app/crm.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
